#include<stdio.h>
#include<math.h>
int main()
{ int a,b,c,d,x,y;

while(1){
scanf("%d",&a);
  scanf("%d %d",&b,&c);

    if(a==0)
        break;
        {


    d=a*b;
   x= (d*100)/c;

   y = pow(x,0.5);
}
printf("%d\n",y);
}
return 0;
}

